{
  "name": "functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for sending task emails",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase serve --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.9.0",
    "nodemailer": "^8.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/nodemailer": "^7.0.11",
    "typescript": "^5.3.3"
  }
}
